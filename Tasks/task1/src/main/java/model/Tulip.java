package model;

import java.time.LocalDate;

public class Tulip extends Flower {
    public Tulip(LocalDate recency, float stemLength, float price) {
        super(recency, stemLength, price);
    }
}
